#include <iostream>
using namespace std;
int main(){

    // &&

    // true

    // int age = 10;
    // int points  = 100;
    // cout << (age >= 5 && points > 50) << endl ; // True => 1

    // false

    // int age = 4;
    // int points  = 60;
    // cout << (age >= 5 && points > 50) << endl ; // False => 0 (Becuase age is smaller than 5)


    // ||

    // int age = 4;
    // int points  = 60;
    // cout << (age >= 5 || points > 50) << endl ; // True => 1 (Becuase one of those is true)

    // int age = 4;
    // int points  = 40;
    // cout << (age >= 5 && points > 50) << endl ; // False => 0 (Becuase all is false)


    // !
    cout << (10 == 10) << endl;                   // True => 1
    cout << (20 != 10) << endl;                   // True => 1
    cout << !(10 == 10) << endl;                   // false => 0
return 0;
}